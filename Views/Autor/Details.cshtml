@model tallerbiblioteca.Models.Autor

@{
    ViewData["Title"] = "Details";
}

<h1>Detalles</h1>

<div>
    <h4>@Model.NombreAutor</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NombreAutor)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NombreAutor)
        </dd>
    </dl>
</div>
<div>
    <button class="mt-2 mb-2">  <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |</button>
    <button><a asp-action="Index">Regresar</a></button>
    
</div>
