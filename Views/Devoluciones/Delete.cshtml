@model tallerbiblioteca.Models.Devolucion

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Devolucion</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Prestamo)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Prestamo.Id)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Observaciones)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Observaciones)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Fecha_devolucion)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Fecha_devolucion)
        </dd>
    </dl>
    
    <form asp-action="Delete" id="formulario" onsubmit="sendForm(event)">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="boton boton-danger" /> |
        <button class="mt-2 botonDelete Deletemedium"> <a asp-action="Index">Regresar</a></button>
        
    </form>
    @section Scripts{
        <script>
            function sendForm(e) {
                e.preventDefault();
                Swal.fire({
                    title: 'Quieres eliminar esta devolucion?',
                    icon: 'question',
                    showCancelButton: true,
                    confirmButtonText: 'SI'
                }).then((resultado) => {
                    if (resultado.isConfirmed) {
                        const formulario = document.getElementById("formulario")
                        formulario.submit();
                    }
                })
            }
        </script>
    }
</div>
